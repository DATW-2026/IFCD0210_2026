name: CI-CD Workflow
on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout code
          uses: actions/checkout@v6

        - name: Set up Node.js
          uses: actions/setup-node@v6
          with:
            node-version: '24.x'

        - name: goto project directory
          run: cd 21.CI.CD # Replace with your actual project directory
        
        - name: Install dependencies
          run: npm install

        - name: Run linting
          run: npm run lint

        - name: Run tests
          run: npm run test:c

        - name: Build project
          run: npm run build

        # - name: Deploy to production
        #   if: github.ref == 'refs/heads/main'
        #   run: echo "Deploying to production..." # Replace with actual deployment command
